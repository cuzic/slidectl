了解。**Python + Playwright + uv + mise** 前提での最新版仕様書をまとめました。APIは使わず、CLI連携のみです。

# slidectl 仕様書（Python/Playwright/uv/mise 版・v1.1）

## 0. 目的

* 原文（Markdown）→ 指示生成（LLM外部CLI）→ Marp.md/SVG → レンダリング（HTML/SVG/PPTX）→ **Playwrightで測定** → しきい値で反復最適化 → PPTX 確定。
* **非LLM**の役割：レンダリング、数値計測、収束管理、ファイル連携、実行オーケストレーション。
* **LLM**（例：`claude-cli`）は外部コマンドとして呼び出し。slidectl 自体は LLM を内包しない。

---

## 1. 採用技術

* 言語: **Python 3.11+**
* パッケージ管理: **uv**
* ランタイム/タスク管理: **mise**
* レンダリング＆DOM計測: **Playwright (Python)**（Chromium）
* スライド変換: **marp-cli**（Node, `npx marp` 利用）
* CLI: **Typer**
* 設定: YAML/JSON（レイアウト辞書・ポリシー・外部コマンドテンプレ）

---

## 2. 環境セットアップ（mise + uv）

### 2.1 mise 設定（ルートに配置）

`.tool-versions`（または `mise.toml`）

```
python 3.11
node 20
```

### 2.2 依存インストール

```bash
# PythonとNodeの取得
mise install

# Pythonパッケージ（uv）
uv venv
uv pip install -U pip
uv pip install typer[all] pydantic==2.* pyyaml rich playwright

# Playwright ブラウザ（Chromium）を取得
python -m playwright install chromium

# marp-cli（Node）
npm i -g @marp-team/marp-cli
# もしくは都度 npx marp を使用
```

> 以降は `mise run` で定義タスクを呼び出せるようにします（後述）。

---

## 3. プロジェクト構成（標準）

```
slidectl/
  src/slidectl/__init__.py
  src/slidectl/cli.py               # Typerエントリ
  src/slidectl/fs.py                # ファイル/WS管理
  src/slidectl/render.py            # marp呼び出し
  src/slidectl/measure.py           # Playwright計測
  src/slidectl/optimize.py          # しきい値評価/反復
  src/slidectl/config.py            # 設定/スキーマ
  src/slidectl/logging.py           # ログ/JSON出力
  templates/                        # LLM用プロンプト（文字列だけ、実行は外部CLI）
  config/
    layouts.yaml
    policy.json
    svg.css
    theme.css
  ws.sample/
    ingest/ ... (空)
README.md
pyproject.toml
mise.toml or .mise/tasks.toml
```

---

## 4. CLI コマンド仕様（Typer）

### 共通

* `--ws <path>`：ワークスペース（既定: `./workspace`）
* `--json`：機械可読ログを **stdout**、人間可読ログを **stderr** に出力
* 返り値：

  * 0: 正常
  * 2: 入力/構文エラー
  * 3: 外部CLI失敗（LLM/ビルド）
  * 4: レンダリング失敗
  * 5: 計測失敗
  * 6: 最適化未収束

### コマンド一覧

1. `slidectl init`

   * 役割: `config/` とWS骨組みを作成
   * 主要オプション: `--force`, `--theme config/theme.css`

2. `slidectl ingest --in doc/raw.md`

   * 役割: Markdown正規化・章/スライド候補抽出
   * 出力: `ingest/normalized.md`, `ingest/structure.json`

3. `slidectl instruct`

   * 役割: **外部LLM CLI**でスライド指示JSON生成
   * 実行: `policy.json.commands.instruct_cmd` を `subprocess` で実行
   * 出力: `instruct/instructions.json`

4. `slidectl build`

   * 役割: **外部LLM CLI**で最終 Marp.md と必要な SVG を生成
   * 実行: `policy.json.commands.build_cmd`
   * 出力: `build/slides.marp.md`, `build/assets/svg/*.svg`

5. `slidectl render`

   * 役割: **marp-cli** で HTML/SVG/PPTX を生成（中間ファイル）
   * 例: `npx marp build/slides.marp.md --html --allow-local-files --theme config/theme.css --output render/slides.html`
   * 備考: SVGページ群も生成して `measure` の入力にする（`--images svg` 相当）

6. `slidectl measure`

   * 役割: **Playwright (Chromium)** で `render/slides.html` を開き、各 `<svg>` を走査
   * 算出指標（各スライド）

     * `density`: テキスト領域合計 / スライド領域
     * `whitespace_ratio`: 1 - （要素の結合BBox面積 / スライド面積）
     * `overlaps`: `<text>/<foreignObject>` の BBox 交差数
   * 出力: `optimize/scorecard.json`

7. `slidectl optimize`

   * 役割: `scorecard.json` を評価し、しきい値逸脱スライドに **パッチ方針** を作成 → **外部LLM CLI** を呼び出して再ビルド → `render`→`measure` を反復
   * 主要オプション: `--max-iter 3`
   * 出力: `optimize/patches.json`, `report/slide_quality_report.csv`

8. `slidectl export`

   * 役割: PPTX の最終出力（フォント・テーマ固定、16:9）
   * 出力: `out/presentation_final.pptx`

9. `slidectl status`

   * 役割: 工程別の完了状況と最新スコアを表示

---

## 5. 設定ファイル仕様

### 5.1 `config/layouts.yaml`（例）

```yaml
layouts:
  text-only:
    max_lines: 18
    prefer_assets: false
  two-column:
    left_max_lines: 10
    right_max_lines: 10
  image-right:
    text_max_lines: 12
    image_width_pct: 40
  quote-slide:
    max_lines: 6
```

### 5.2 `config/policy.json`（例）

```json
{
  "density_range": [0.012, 0.018],
  "whitespace_range": [0.15, 0.40],
  "max_iterations": 3,
  "commands": {
    "instruct_cmd": "claude-cli run --prompt templates/instruct.prompt --var structure=@ingest/structure.json --out instruct/instructions.json",
    "build_cmd": "claude-cli run --prompt templates/build.prompt --var instructions=@instruct/instructions.json --out build/slides.marp.md",
    "augment_cmd": "claude-cli run --prompt templates/augment.prompt --var patches=@optimize/patches.json --out build/slides.marp.md"
  }
}
```

---

## 6. Playwright 計測仕様（要点）

### 6.1 レンダリング入力

* `render/slides.html`（marp-cli で生成）
* スライド単位 `<section>` or `<svg>` を列挙

### 6.2 計測アルゴリズム（概略）

1. スライド全体BBox（`svg.viewBox`）を取得
2. 対象要素集合：`text, foreignObject`（必要に応じ `tspan` 集約）
3. 各要素の `getBBox()` を取得
4. **density** = `Σ (w×h) / slide_area`
5. **union area** を近似（掃き出し線または格子近似で十分精度を確保）
6. **whitespace_ratio** = `1 - union_area / slide_area`
7. **overlaps** = BBox ペアの交差数（しきい値に応じて1px超をカウント）
8. 結果を `optimize/scorecard.json` へ（スライドID、指標、警告）

> 目安しきい値：`density 0.012–0.018`、`whitespace 0.15–0.40`、`overlaps 0`

---

## 7. 反復最適化（optimize）ポリシー

* **過密（density>0.018 or 行数>layout.max）**

  1. 箇条書き短縮 2) 改行再配置 3) 用語統合 4) スライド分割（a/b）
* **余白過多（whitespace>0.40）**

  1. 文章補強（背景/効果/次アクション 最大3行）
  2. 説明用SVG（diagram/flow/timeline/matrix）追加
  3. レイアウト変更（text-only→image-right/two-column）
* **重なり>0**

  1. 強制改行/行間 2) 箇条書き項目削減 3) 分割

`optimize` は上記方針に沿った **Patch JSON** を作成し、`policy.commands.augment_cmd` を叩いて LLM 側で Marp.md を再生成 → `render`→`measure` を再実行。`--max-iter` 到達で未収束なら `exit 6`。

---

## 8. marp-cli 呼び出し（render/export）

* HTML 中間生成：

  ```bash
  npx marp build/slides.marp.md \
    --html --allow-local-files \
    --theme config/theme.css \
    --output render/slides.html
  ```

* PPTX 最終出力（フォントはテーマ側で指定 or システム導入）：

  ```bash
  npx marp build/slides.marp.md \
    --pptx --allow-local-files \
    --theme config/theme.css \
    --output out/presentation_final.pptx
  ```

> Node は **mise** 管理。CI でも再現性が高い。

---

## 9. mise タスク（開発/運用ショートカット）

`mise.toml`（抜粋）

```toml
[tasks.init]
run = "uv run slidectl init --ws ws"

[tasks.pipeline]
run = """
uv run slidectl ingest --ws ws --in doc/raw.md \
&& uv run slidectl instruct --ws ws \
&& uv run slidectl build --ws ws \
&& uv run slidectl render --ws ws \
&& uv run slidectl measure --ws ws \
&& uv run slidectl optimize --ws ws --max-iter 3 \
&& uv run slidectl export --ws ws --pptx-name talk_2025.pptx
"""

[tasks.status]
run = "uv run slidectl status --ws ws --json | jq ."
```

> 端末では `mise run pipeline` で一発実行。

---

## 10. カスタム・スラッシュコマンド

### 10.1 ローカル「疑似スラッシュ」

```bash
/slide() { uv run slidectl "$@"; }
# 使用例:
/slide ingest --ws ws --in doc/raw.md
/slide optimize --ws ws --max-iter 3
/slide export --ws ws --pptx-name talk_2025.pptx
```

### 10.2 Slack スラッシュ（任意）

* `/slide run ws=talk in=gs://... name=talk_2025.pptx`
* 受け口は簡易HTTPで `mise run pipeline` を起動し、完了時にスレッド通知。

---

## 11. 成功判定（受け入れ基準）

* `overlaps == 0`（全スライド）
* `density` は 95%以上のスライドで **0.012–0.018**
* `whitespace_ratio` は 95%以上のスライドで **0.15–0.40**
* `optimize` が `--max-iter` 未満で収束（`exit 0`）
* `out/presentation_final.pptx` 生成成功

---

## 12. リスク/対策

* **フォント差異**：CI/本番での表示差はテーマで明示指定・フォント導入手順を同梱
* **SVG座標の取得誤差**：`getBBox()` 未対応要素は `getBoundingClientRect()` 併用
* **LLM外部CLI失敗**：戻り値と標準エラーを `slidectl` ログに保存、再実行可能に
* **marpテーマ変更**：`layouts.yaml`／`policy.json` のしきい値をテーマ別プロファイル化

---

## 13. 次アクション（提案）

1. ひな形リポジトリを作成（上記レイアウト）
2. `slidectl init / ingest / render / measure` の**最小実装**
3. `mise run pipeline` で HTML/scorecard まで通るか確認
4. `instruct/build/augment` の外部CLIテンプレ接続
5. `optimize` の反復制御を実装 → `export` 仕上げ

---

必要なら、この仕様に沿った **`pyproject.toml` 雛形** と **Typer エントリ骨子**, **Playwright 計測の疑似コード（Python）** もすぐ出せます。

承知しました。
以下は、これまで定義した要件・ユーザーストーリーを踏まえた
**「slidectl 受入条件ドキュメント（Acceptance Criteria v1.0）」** です。

本ドキュメントは、**開発完了判定・品質保証・ユーザ受入試験（UAT）** の基準として使用できます。

---

# ✅ slidectl 受入条件ドキュメント（Acceptance Criteria v1.0）

## 1. 目的

本ドキュメントは、CLIツール **slidectl（Python + Playwright + uv + mise）** の成果物に対して、
開発者および利用者（講師・エンジニア・研修運営担当）が「完成」とみなすための
**機能的・非機能的な受入基準**を明確にすることを目的とする。

---

## 2. 前提条件

| 項目    | 内容                                                     |
| ----- | ------------------------------------------------------ |
| システム名 | slidectl                                               |
| バージョン | v1.0                                                   |
| 実行環境  | macOS/Linux/WSL2 (Python 3.11+, Node 20+, uv, mise)    |
| 使用ツール | marp-cli, Playwright (Chromium), claude-cli（外部LLM CLI） |
| 開発言語  | Python（TyperベースCLI）                                    |
| 実行方法  | `mise run pipeline` または `/slide run`                   |
| 入力    | Markdown原稿（.md）                                        |
| 出力    | PPTX, HTML, JSON, CSV                                  |

---

## 3. 機能別受入条件

### 3.1 `slidectl init`

| No        | 受入基準                                         | 試験方法                             |
| --------- | -------------------------------------------- | -------------------------------- |
| F-INIT-01 | コマンド実行で `config/` とワークスペースが生成される             | CLIで `slidectl init` 実行しファイル存在確認 |
| F-INIT-02 | 既存ファイルがある場合、`--force` オプションで上書き可能            | テスト用既存WSを作り確認                    |
| F-INIT-03 | `layouts.yaml`、`policy.json` がテンプレート内容で生成される | ファイル内容が既定フォーマット通り                |

---

### 3.2 `slidectl ingest`

| No       | 受入基準                                       | 試験方法                               |
| -------- | ------------------------------------------ | ---------------------------------- |
| F-ING-01 | Markdown入力を正規化し、構造化データ(JSON)が出力される         | `ingest/structure.json` のJSON構造を確認 |
| F-ING-02 | セクション・スライド単位に分割される                         | `structure.json` の配列数と原文見出し対応を確認   |
| F-ING-03 | 正規化ファイル（`normalized.md`）がUTF-8 BOMなしで保存される | エンコーディング確認                         |

---

### 3.3 `slidectl instruct`

| No       | 受入基準                                                | 試験方法            |
| -------- | --------------------------------------------------- | --------------- |
| F-INS-01 | `policy.json.commands.instruct_cmd` に基づいて外部CLIを呼び出す | subprocessログを確認 |
| F-INS-02 | 出力 `instructions.json` が生成される                       | ファイル存在確認        |
| F-INS-03 | LLM失敗時はexit code 3を返し、再実行可能                         | 強制エラーで挙動確認      |

---

### 3.4 `slidectl build`

| No       | 受入基準                          | 試験方法                   |
| -------- | ----------------------------- | ---------------------- |
| F-BLD-01 | `build/slides.marp.md` が生成される | ファイル存在確認               |
| F-BLD-02 | `assets/svg/` に1枚以上のSVGが生成される | SVGファイル存在確認            |
| F-BLD-03 | 出力ログにスライド数が記録される              | `build.log.json` の内容確認 |

---

### 3.5 `slidectl render`

| No       | 受入基準                           | 試験方法                       |
| -------- | ------------------------------ | -------------------------- |
| F-RND-01 | marp-cliによってHTMLが生成される         | `render/slides.html` の存在確認 |
| F-RND-02 | marpエラー時はexit code 4を返す        | 不正なMarpファイルでテスト            |
| F-RND-03 | `--format svg` 指定時にSVG出力が生成される | SVGファイル確認                  |
| F-RND-04 | HTMLのテーマ・フォントが設定通りである          | `<style>` 内CSS確認           |

---

### 3.6 `slidectl measure`

| No       | 受入基準                                                   | 試験方法           |
| -------- | ------------------------------------------------------ | -------------- |
| F-MSR-01 | Playwrightが正常起動し、`slides.html`を解析できる                   | Chromium起動ログ確認 |
| F-MSR-02 | `optimize/scorecard.json` が生成される                       | JSONファイル存在確認   |
| F-MSR-03 | 各スライドに `density`, `whitespace_ratio`, `overlaps` が含まれる | JSONキー確認       |
| F-MSR-04 | 同一HTMLで複数回実行しても結果が安定（誤差±5%以内）                          | 再実行差分比較        |

---

### 3.7 `slidectl optimize`

| No       | 受入基準                                            | 試験方法                  |
| -------- | ----------------------------------------------- | --------------------- |
| F-OPT-01 | `scorecard.json` のNGスライドを検出し、`patches.json` を生成 | JSON出力確認              |
| F-OPT-02 | LLM再呼び出しにより再ビルドが行われる                            | subprocessログ確認        |
| F-OPT-03 | 収束判定により最大3回で終了                                  | `--max-iter 3` 実行結果確認 |
| F-OPT-04 | 最終結果が `report/slide_quality_report.csv` に出力される  | CSVファイル内容確認           |
| F-OPT-05 | 未収束時はexit code 6を返す                             | 意図的に過密スライドでテスト        |

---

### 3.8 `slidectl export`

| No       | 受入基準                      | 試験方法        |
| -------- | ------------------------- | ----------- |
| F-EXP-01 | PPTXが `out/` ディレクトリに生成される | ファイル存在確認    |
| F-EXP-02 | フォントとテーマが適用されている          | 開いたときの見た目確認 |
| F-EXP-03 | 生成完了時にステータス0を返す           | 終了コード確認     |

---

### 3.9 `slidectl status`

| No       | 受入基準                       | 試験方法       |
| -------- | -------------------------- | ---------- |
| F-STA-01 | 各工程の進捗・完了状況を表示             | CLI出力確認    |
| F-STA-02 | `--json` オプションで機械可読出力が得られる | JSON構造確認   |
| F-STA-03 | 欠落工程があれば⚠️マーク付きで出力         | 出力フォーマット確認 |

---

## 4. ワークフロー受入条件（E2E）

| No    | 受入基準                                            | 検証方法                     |
| ----- | ----------------------------------------------- | ------------------------ |
| WF-01 | `mise run pipeline` が ingest → export まで自動実行される | 実行後の成果物確認                |
| WF-02 | `presentation_final.pptx` が生成される                | ファイル存在確認                 |
| WF-03 | `scorecard.json` の各スライドが許容範囲内                   | density/whitespaceがしきい値内 |
| WF-04 | overlaps=0（重なりなし）                               | 全スライドの結果確認               |
| WF-05 | optimize反復回数が3以内で収束                             | ログ・終了コード確認               |
| WF-06 | すべてのログにエラー（exit code ≠ 0）が存在しない                 | 実行ログ確認                   |
| WF-07 | `/slide run`（スラッシュ）からの実行でも同結果                   | Slackまたはローカル疑似スラッシュで確認   |

---

## 5. 非機能受入条件

| No    | 要件カテゴリ | 受入基準                             | 検証方法      |
| ----- | ------ | -------------------------------- | --------- |
| NF-01 | 性能     | 100スライド規模のHTML測定を3分以内に完了         | 計測タイマーで確認 |
| NF-02 | 再現性    | `mise run pipeline` により同一環境再実行可能 | 他環境で同結果確認 |
| NF-03 | 移植性    | macOS / Ubuntu / WSL2 で動作確認済     | 各環境テスト    |
| NF-04 | 安定性    | 連続5回実行しても異常終了しない                 | stressテスト |
| NF-05 | 可観測性   | 各工程のログファイルが残る（stdout+stderr）     | logフォルダ確認 |
| NF-06 | データ整合性 | 生成物の内部リンク・画像パスが相対パスで解決           | HTML開いて確認 |
| NF-07 | セキュリティ | 外部通信は LLM CLI 呼び出し時のみ            | ソース確認     |

---

## 6. 品質基準（Quantitative Criteria）

| 指標                  | 目標値   | 許容値    |
| ------------------- | ----- | ------ |
| overlaps（重なり）       | 0     | 0      |
| density範囲内スライド比率    | ≥95%  | ≥90%   |
| whitespace範囲内スライド比率 | ≥95%  | ≥90%   |
| optimize収束率         | ≥95%  | ≥90%   |
| marp-cliエラー率        | 0%    | ≤1%    |
| export成功率           | 100%  | 100%   |
| measure誤差（再実行差）     | ±5%以内 | ±10%以内 |

---

## 7. 受入手順

| ステップ | 作業内容                      | 責任者   |
| ---- | ------------------------- | ----- |
| 1    | 開発環境の mise install 実行     | 開発担当  |
| 2    | `mise run pipeline` を実行   | テスター  |
| 3    | 各成果物ファイルを確認               | テスター  |
| 4    | `scorecard.json` の値を基準内確認 | QA担当  |
| 5    | PPTXを開いて視覚的整合性を確認         | 利用者代表 |
| 6    | 受入報告書（チェックリスト）提出          | QA担当  |

---

## 8. 受入可否判断基準

| 判定            | 条件                             |
| ------------- | ------------------------------ |
| ✅ **合格**      | 全主要機能が動作し、KPIが目標値を満たす          |
| ⚠️ **条件付き合格** | 一部指標が許容値内に収まるが、軽微な修正で改善可能      |
| ❌ **不合格**     | 主要機能未実装 or 測定誤差・密度指標が閾値外（>10%） |

---

## 9. 付録：成果物確認チェックリスト

| 項目            | 出力パス                            | 確認結果 |
| ------------- | ------------------------------- | ---- |
| Marp Markdown | build/slides.marp.md            | ☐    |
| HTMLレンダリング    | render/slides.html              | ☐    |
| 測定結果JSON      | optimize/scorecard.json         | ☐    |
| 品質レポート        | report/slide_quality_report.csv | ☐    |
| 最終PPTX        | out/presentation_final.pptx     | ☐    |
| ログファイル        | logs/*.log                      | ☐    |

---

## 10. 承認セクション

| 承認者         | 役割        | 日付         | サイン     |
| ----------- | --------- | ---------- | ------- |
| 🧑‍💻 川西智也  | プロジェクト責任者 | yyyy/mm/dd | ＿＿＿＿＿＿＿ |
| 🧠 QA担当者    | 品質保証      | yyyy/mm/dd | ＿＿＿＿＿＿＿ |
| 🧑‍🏫 利用代表者 | 最終承認      | yyyy/mm/dd | ＿＿＿＿＿＿＿ |

---

✅ **この受入条件を満たした時点で、slidectl v1.0 の正式リリースとする。**

---

次に作成可能なのは：

1. **受入試験項目表（テストケース定義書）**
2. **品質KPIダッシュボード仕様（CSV→可視化）**
3. **CLIエラーコード設計書**

どれを続けて作成しますか？
